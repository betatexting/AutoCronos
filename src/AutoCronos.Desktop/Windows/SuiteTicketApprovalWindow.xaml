<local:ChromeWindow
    x:Class="AutoCronos.Desktop.Windows.SuiteTicketApprovalWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:AutoCronos.Desktop.Windows"
    Title="AutoCronos | Chamados"
    Width="680"
    Height="680"
    MinWidth="540"
    MinHeight="500"
    WindowStyle="None"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner"
    Background="#000000"
    FontFamily="./Assets/Fonts/#Bebas Neue">
    <Border BorderBrush="#171717" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border Background="Transparent" MouseLeftButtonDown="ChromeTitleBar_MouseLeftButtonDown" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,0">
                    <Button Style="{StaticResource ChromeWindowControlButton}" Content="−" Click="ChromeMinimize_Click" ToolTip="Minimizar" />
                    <Button Style="{StaticResource ChromeWindowControlButton}" Content="□" FontSize="15" Click="ChromeMaximizeRestore_Click" ToolTip="Maximizar ou restaurar" />
                    <Button Style="{StaticResource ChromeWindowControlButton}" Content="×" FontSize="20" Margin="0" Click="ChromeClose_Click" ToolTip="Fechar" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="24,0,24,22">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="CHAMADOS" Foreground="#F7F7F7" FontSize="22" />
                    <TextBlock x:Name="ActivityTextBlock" Margin="0,4,0,0" Foreground="#B5B5B5" FontSize="11" TextWrapping="Wrap" />
                </StackPanel>

                <Border Grid.Row="1" Margin="0,16,0,12" Padding="12,9" Background="#202E28" CornerRadius="5">
                    <TextBlock Text="O card entrou em andamento. Revise os dados antes de criar os chamados no Suite360." Foreground="#D9EFE1" FontSize="10" TextWrapping="Wrap" />
                </Border>

                <StackPanel Grid.Row="2">
                    <TextBlock Text="Dados carregados do Suite360" Foreground="#E0E0E0" FontSize="11" />
                    <TextBlock x:Name="LoadStatusTextBlock" Margin="0,3,0,0" Foreground="#9A9A9A" FontSize="9" Text="Carregando dados do Suite360..." />
                    <Grid Margin="0,6,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,5,0">
                            <TextBlock Text="Tipo" Foreground="#9A9A9A" FontSize="9" />
                            <ComboBox x:Name="TicketTypeComboBox" Margin="0,3,0,0" Padding="6,4" DisplayMemberPath="Name" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0">
                            <TextBlock Text="Origem" Foreground="#9A9A9A" FontSize="9" />
                            <ComboBox x:Name="OriginComboBox" Margin="0,3,0,0" Padding="6,4" DisplayMemberPath="Name" />
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="5,0">
                            <TextBlock Text="Setor" Foreground="#9A9A9A" FontSize="9" />
                            <ComboBox x:Name="SectorComboBox" Margin="0,3,0,0" Padding="6,4" DisplayMemberPath="Name" SelectionChanged="Sector_SelectionChanged" />
                        </StackPanel>
                        <StackPanel Grid.Column="3" Margin="5,0,0,0">
                            <TextBlock Text="Executor" Foreground="#9A9A9A" FontSize="9" />
                            <ComboBox x:Name="ExecutorComboBox" Margin="0,3,0,0" Padding="6,4" DisplayMemberPath="Name" />
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="Titulo" Margin="0,10,0,0" Foreground="#9A9A9A" FontSize="9" />
                    <TextBox x:Name="TicketTitleTextBox" Margin="0,3,0,0" Padding="8,6" />
                    <TextBlock Text="Descricao" Margin="0,8,0,0" Foreground="#9A9A9A" FontSize="9" />
                    <TextBox x:Name="TicketDescriptionTextBox" Margin="0,3,0,0" Height="54" Padding="8,6" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
                </StackPanel>

                <TextBlock Grid.Row="3" x:Name="SelectionHintTextBlock" Margin="0,15,0,0" Foreground="#E0E0E0" FontSize="11" TextWrapping="Wrap" />

                <ListBox x:Name="CustomersListBox"
                         Grid.Row="4"
                         Margin="0,10,0,0"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Hidden"
                         ScrollViewer.CanContentScroll="False"
                         PreviewMouseWheel="Customers_PreviewMouseWheel">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,8" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12,9" Background="#2D2D2D" CornerRadius="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                        <TextBlock Text="{Binding CompanyName}" Foreground="#F2F2F2" FontSize="11" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding TaxId}" Margin="0,2,0,0" Foreground="#9A9A9A" FontSize="9" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                    <Button Content="Cancelar" IsCancel="True" Margin="0,0,8,0" Style="{StaticResource FormSecondaryButton}" />
                    <Button x:Name="CreateTicketsButton" Content="Criar chamados" Click="CreateTickets_Click" Style="{StaticResource FormPrimaryButton}" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</local:ChromeWindow>
