<Window
    x:Class="AutoCronos.Desktop.Windows.TextPromptWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Nova coluna"
    Width="460"
    Height="235"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    Background="#F5F7FA">
    <Grid Margin="26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock x:Name="HeadingTextBlock" Text="Nova coluna" FontSize="22" FontWeight="SemiBold" Foreground="#172033" />
            <TextBlock x:Name="PromptTextBlock" Text="Informe o nome da coluna." Margin="0,5,0,14" Foreground="#61708A" />
        </StackPanel>
        <TextBox x:Name="ValueTextBox" Grid.Row="1" Padding="9,7" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Cancelar" IsCancel="True" Padding="14,7" Margin="0,0,8,0" Background="White" BorderBrush="#D6DCE6" />
            <Button Content="Adicionar" IsDefault="True" Click="Confirm_Click" Padding="16,7" Background="#153A5B" Foreground="White" BorderThickness="0" />
        </StackPanel>
    </Grid>
</Window>
